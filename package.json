{
  "name": "languages-db",
  "version": "1.0.0",
  "description": "",
  "type": "module",
  "scripts": {
    "prebuild": "rm -rf dist",
    "build": "for lang in src/*/; do mkdir -p dist/$(basename $lang) && cp -R \"$lang/assets/\"* \"dist/$(basename $lang)/\"; done",
    "test": "node scripts/validate.ts && vitest run"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^25.3.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3",
    "vitest": "^4.0.18"
  }
}
